/*
 * 折叠窗口大小
 */
export const WIN_FOLD_SIZE = {
  // 默认折叠窗口尺寸
  foldWinSize: {
    width: 100,
    height: 100
  }
}

/*
 * 折叠窗口可配置参数
 */

export const WIN_FOLD_SET = {
  exclusiveWithMainWin: false // 折叠窗口是否与主窗口互斥
}

export const windowSize = {
  width: 700,
  height: 500
}
